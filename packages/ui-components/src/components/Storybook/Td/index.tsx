import { styled } from '@storybook/theming'

const Td = styled.td`
  padding: 24px 0;
  border-bottom: 1px solid rgba(0, 0, 0, 0.08);

  &:first-child {
    padding-right: 120px;
  }
`

export default Td
